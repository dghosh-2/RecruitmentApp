{
  "name": "recruiterproupdated",
  "version": "1.0.0",
  "private": true,
  "description": "AI-first job listing tracker for students and professionals",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,cyan \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "setup": "npm install && npx playwright install chromium",
    "migrate": "npm run migrate --workspace backend",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
